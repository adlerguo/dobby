# QA Artifacts

This directory contains QA-only artifacts for test execution.

- `records/`: step-by-step test records and findings.
- `scripts/`: temporary QA helper scripts and curl collections.
- `env/`: runtime-only files such as tokens, accounts, and baseline snapshots.

Files under `qa/` are test artifacts and do not change business code or product configuration.
